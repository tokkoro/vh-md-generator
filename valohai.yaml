- step:
    name: create-many-outputs
    image: alpine
    command:
      - echo {parameters}
    parameters:
      - name: how_many
        description: How many outputs do you want?
        type: integer
        default: 20
      - name: word
        type: string
        choices:
          - cool
          - wait
          - check
          - who