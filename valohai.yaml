---

- step:
    name: Execute python train.py
    image: python
    command: python train.py {parameters}
    parameters:
      - name: files
        description: How many files do you want?
        type: integer
        default: 10
